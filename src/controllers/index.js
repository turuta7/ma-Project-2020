const users = require('./users');
const carsUsers = require('./carsUsers');
const routes = require('./routes');
const trips = require('./trips');

module.exports = { users, carsUsers, routes, trips };
